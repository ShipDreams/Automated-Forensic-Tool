# Android 自动取证 MVP 项目依赖

# Desktop notification (cross-platform)
plyer>=2.1.0

# GUI launcher (pywebview)
pywebview>=4.0

# Python 版本要求
# 推荐并验证通过: Python 3.10.x
# 交付环境请固定使用 Python 3.10.11

# 系统依赖
# - adb (Android Debug Bridge)
#   Mac 安装: brew install --cask android-platform-tools
#   Linux 安装: apt-get install android-tools-adb
#   Windows 安装: 下载 Android SDK Platform Tools
#
# - libnotify (Linux only, for desktop notifications)
#   Linux 安装: apt-get install libnotify-bin

# Image recognition (OpenCV)
opencv-python>=4.8.0
numpy>=1.24.0
uiautomator2==3.2.2

# Database (MySQL)
pymysql>=1.1.0

# OCR
paddleocr==3.3.0
paddlepaddle==3.2.0
scipy>=1.10.0
scikit-image>=0.22.0
Pillow>=10.0.0
PyYAML>=6.0
pyclipper>=1.3.0
shapely>=2.0.0
imagesize>=1.4.0
pypdfium2>=4.0.0
python-bidi>=0.6.0
ruamel.yaml>=0.18.0
ujson>=5.0.0
pandas>=2.0.0
prettytable>=3.0.0
py-cpuinfo>=9.0.0
modelscope>=1.28.0
huggingface-hub>=0.20.0
filelock>=3.0.0
colorlog>=6.0.0
chardet>=5.0.0
aistudio-sdk>=0.3.5

# Windows packaging - optional
# pyinstaller>=5.0

# 可选依赖（用于开发和测试）
# pytest>=7.0.0
# black>=22.0.0
